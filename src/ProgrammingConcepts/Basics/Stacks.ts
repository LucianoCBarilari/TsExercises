/*
*
Stacks (LIFO - Last In, First Out)
Description:
A stack is a data structure that follows the LIFO principle. Elements are added (pushed) and removed (popped) from the same end, known as the "top" of the stack.

Problem:
Implement a stack using an array and provide the following operations:

Push an element onto the stack.

Pop an element from the stack.

Check if the stack is empty.

Display the top element of the stack.
 */