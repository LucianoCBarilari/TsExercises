import { Person } from '../../Models/Person';
/*
Arrays (Arreglos)
Description:
Arrays are fixed-size collections of elements of the same data type. Each element can be accessed by its index.

Problem:
Write a program that takes 10 integers from the user, stores them in an array, and then prints the sum of all the elements.
*/
let arrayExample : Array<Person> = [
    new Person ( 'John', 30) ,
    new Person ( 'Jane', 25) ,
    new Person ( 'Jim', 40)
];
